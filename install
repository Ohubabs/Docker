#Run

sudo apt install docker.io

#Check version

sudo docker version

#Add to Docker group 

sudo usermod -aG docker ubuntu #<username>

#Restart Docker Service or exit and re-login to server for group addition to take place.

sudo systemctl restart docker.service
